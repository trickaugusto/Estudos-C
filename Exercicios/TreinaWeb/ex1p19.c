#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numero;
    printf("Digite o numero desejado: ");
    scanf("%d", numero);
    int i;
    while (i = numero, i = 1, i --){
        printf("%d * ", i);

    }
    return 0;
}
